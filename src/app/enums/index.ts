export enum Order {
  Desc = 'desc',
  Asc = 'asc',
}

export enum HavePagination {
  Y = 'Y',
  N = 'N',
}

export enum ProgrammingLanguage {
  Javascript = 0,
  Python = 2,
  Java = 3,
}

export enum ActiveStatus {
  Active = '1',
  Inactive = '0',
}
